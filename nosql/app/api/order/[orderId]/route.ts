import { NextRequest, NextResponse } from "next/server";

/* //update order status only naja
export async function PATCH(req:NextRequest, context:{params:Promise<{orderId:string}>}){
    const {orderId} = await context.params
    try {
        
    } catch (error) {
        
    }
}
*/
